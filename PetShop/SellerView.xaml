<UserControl x:Class="PetShop.SellerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PetShop"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400" MinWidth="400">
    
    <DockPanel>

        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" DockPanel.Dock="Top">
            <TextBlock Text="Seller Home Page" FontSize="20" FontStyle="Italic" Margin="0,20"></TextBlock>
            <Button Content="Add Pet" Command="{Binding AddToCartCommand}" Style="{StaticResource buttonStyle}"/>
            <Button Content="Remove Pet" Command="{Binding ViewCartCommand}" Style="{StaticResource buttonStyle}"/>
            <Button Content="Edit Pet" Command="{Binding CheckoutCommand}" Style="{StaticResource buttonStyle}"/>
            <Button Content="Change Theme" Command="{Binding CheckoutCommand}" Style="{StaticResource buttonStyle}"/>
        </StackPanel>

    </DockPanel>
</UserControl>
